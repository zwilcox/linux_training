#ifndef _AVLIBBUF_H
#define _AVLIBBUF_H

extern unsigned char antiviruslib_so[];
extern unsigned int antiviruslib_so_len;

// The above get put into avlibbuf.c which is created by xxd -i on antiviruslib.so

#endif
